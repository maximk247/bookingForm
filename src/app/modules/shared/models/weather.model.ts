export class Weather {
    temperature: number;
    cloudType: number;

    constructor(temperature: number, cloudType: number) {
        this.temperature = temperature;
        this.cloudType = cloudType;
    }
}
